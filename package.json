{
  "name": "lint-rules",
  "type": "module",
  "scripts": {
    "build": "vp run -r build",
    "fmt": "vp run -r fmt",
    "test": "vp run -r test",
    "lint": "vp run -r lint",
    "pr": "vp run -r fmt && vp run -r build && vp run -r lint && vp run -r test"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "packageManager": "pnpm@10.33.0"
}
