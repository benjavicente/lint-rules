# No Resource API

Disallow Angular `resource`, `rxResource`, and `httpResource`.

## Why

For server state, prefer a dedicated server-state library such as TanStack Query
instead of Angular resource APIs.
